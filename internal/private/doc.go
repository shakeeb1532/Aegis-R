//go:build private

// Package private contains enterprise-only or IP-sensitive implementations.
//
// This package is excluded from default builds and should be compiled only when
// the `private` build tag is provided.
package private
