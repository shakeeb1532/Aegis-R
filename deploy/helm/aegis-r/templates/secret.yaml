{{- if and .Values.ui.enabled .Values.ui.basicPassSecretCreate }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ui.basicPassSecret }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  password: "{{ .Values.ui.basicPass }}"
{{- end }}
